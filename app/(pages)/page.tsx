//export const revalidate = 10;

import ScrollingFeed from "@/comps/feed/ScrollingFeed";

const Home = async () => {
  return (
    <section>
      <ScrollingFeed />
    </section>
  );
};

export default Home;
